---
phase: 01-migrate-sora-to-kling-ai
plan: 05
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - messages/es.json
  - messages/en.json
autonomous: true
requirements:
  - ERR-01

must_haves:
  truths:
    - "messages/es.json has a top-level 'errors.kling' object with five keys: rateLimit, contentPolicy, modelUnavailable, generationFailed, unknownError"
    - "messages/en.json has matching keys with English translations"
    - "Both JSON files are valid (parseable) after modification"
    - "Existing keys in both files are unchanged"
  artifacts:
    - path: "messages/es.json"
      provides: "Spanish error messages for Kling API errors"
      contains: "errors.kling.rateLimit"
    - path: "messages/en.json"
      provides: "English error messages for Kling API errors"
      contains: "errors.kling.rateLimit"
  key_links:
    - from: "app/api/generate/route.ts"
      to: "messages/es.json + messages/en.json"
      via: "error key string 'errors.kling.rateLimit' looked up by next-intl at render time"
      pattern: "errors.kling"
---

<objective>
Add errors.kling.* i18n keys to messages/es.json and messages/en.json. These are the human-readable error messages surfaced to users when Kling API returns rate limit, content policy, model unavailable, or unknown errors.

Purpose: Plan 03 (generate route) returns error key strings like "errors.kling.rateLimit" in the response. The client-side UI (next-intl) looks up these keys to display the translated message. Without the keys, users see the raw key string instead of a readable message.

Output: messages/es.json and messages/en.json with errors.kling.* block added. Both files remain valid JSON.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-migrate-sora-to-kling-ai/01-CONTEXT.md
@.planning/phases/01-migrate-sora-to-kling-ai/01-RESEARCH.md

<interfaces>
<!-- Current i18n file structure. Extracted from codebase. -->

messages/es.json current top-level keys (in order):
  "common", "navigation", "home", "form", "generate", "credits",
  "creditPackages", "auth", "profile", "videoList"

messages/en.json current top-level keys (in order):
  "common", "navigation", "home", "form", "generate", "credits",
  "creditPackages", "auth", "profile", "videoList"

There is NO existing top-level "errors" key in either file.

Error key strings returned by app/api/generate/route.ts (from plan 03):
  "errors.kling.rateLimit"
  "errors.kling.contentPolicy"
  "errors.kling.modelUnavailable"
  "errors.kling.unknownError"
  (also: "errors.kling.generationFailed" — for webhook-triggered failures surfaced to UI)

Spanish text (from RESEARCH.md code examples — verified):
  rateLimit: "Has alcanzado el límite de solicitudes. Por favor, intenta nuevamente en unos minutos."
  contentPolicy: "Tu solicitud fue rechazada por la política de contenido. Por favor, modifica el prompt e intenta nuevamente."
  modelUnavailable: "El modelo de generación de video no está disponible temporalmente. Por favor, intenta nuevamente en unos minutos."
  generationFailed: "La generación de video falló. Por favor, intenta nuevamente."
  unknownError: "Ocurrió un error al generar el video. Por favor, intenta nuevamente."

English text (from RESEARCH.md code examples — verified):
  rateLimit: "You have reached the request limit. Please try again in a few minutes."
  contentPolicy: "Your request was rejected by the content policy. Please modify your prompt and try again."
  modelUnavailable: "The video generation model is temporarily unavailable. Please try again in a few minutes."
  generationFailed: "Video generation failed. Please try again."
  unknownError: "An error occurred while generating the video. Please try again."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add errors.kling.* keys to messages/es.json</name>
  <files>messages/es.json</files>

  <read_first>
    - messages/es.json (read full file — must see current structure to insert correctly without breaking JSON validity)
  </read_first>

  <action>
    Read messages/es.json. The file ends with the "videoList" object and then the closing `}` of the root object.

    Before the final closing `}` of the root object, add a comma after the "videoList" block's closing `}`, then add the new "errors" top-level key:

    ```json
    "errors": {
      "kling": {
        "rateLimit": "Has alcanzado el límite de solicitudes. Por favor, intenta nuevamente en unos minutos.",
        "contentPolicy": "Tu solicitud fue rechazada por la política de contenido. Por favor, modifica el prompt e intenta nuevamente.",
        "modelUnavailable": "El modelo de generación de video no está disponible temporalmente. Por favor, intenta nuevamente en unos minutos.",
        "generationFailed": "La generación de video falló. Por favor, intenta nuevamente.",
        "unknownError": "Ocurrió un error al generar el video. Por favor, intenta nuevamente."
      }
    }
    ```

    Rules:
    - The final structure must be valid JSON — exactly one comma between each top-level key
    - The "videoList" block needs a comma after its closing `}` to separate from "errors"
    - The new "errors" block must NOT have a trailing comma after its closing `}`
    - Do NOT modify any existing keys or values
    - Use Write tool to overwrite the file with the complete new content (not a patch)
  </action>

  <verify>
    Run: `node -e "const f = require('./messages/es.json'); console.log(JSON.stringify(f.errors.kling))" 2>&1`

    Expected output: JSON string with all five kling error keys (rateLimit, contentPolicy, modelUnavailable, generationFailed, unknownError).

    Run: `node -e "JSON.parse(require('fs').readFileSync('./messages/es.json', 'utf8')); console.log('Valid JSON')" 2>&1`

    Expected: "Valid JSON"
  </verify>

  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('./messages/es.json', 'utf8')); console.log('ok')"` outputs "ok" (valid JSON)
    - `grep "errors" messages/es.json | grep -c "kling"` returns at least 1
    - `grep '"rateLimit"' messages/es.json` returns a result with Spanish text
    - `grep '"contentPolicy"' messages/es.json` returns a result with Spanish text
    - `grep '"modelUnavailable"' messages/es.json` returns a result with Spanish text
    - `grep '"generationFailed"' messages/es.json` returns a result with Spanish text
    - `grep '"unknownError"' messages/es.json` returns a result with Spanish text
    - `grep '"videoList"' messages/es.json` still returns a result (existing key preserved)
    - `grep '"common"' messages/es.json` still returns a result (existing key preserved)
  </acceptance_criteria>

  <done>
    messages/es.json contains errors.kling with five Spanish error messages. File is valid JSON. All existing keys unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add errors.kling.* keys to messages/en.json</name>
  <files>messages/en.json</files>

  <read_first>
    - messages/en.json (read full file — same structure as es.json but may have slight formatting differences)
  </read_first>

  <action>
    Read messages/en.json. Apply the same change as es.json: add a top-level "errors" key after "videoList".

    Before the final closing `}` of the root object, add a comma after the "videoList" block's closing `}`, then add:

    ```json
    "errors": {
      "kling": {
        "rateLimit": "You have reached the request limit. Please try again in a few minutes.",
        "contentPolicy": "Your request was rejected by the content policy. Please modify your prompt and try again.",
        "modelUnavailable": "The video generation model is temporarily unavailable. Please try again in a few minutes.",
        "generationFailed": "Video generation failed. Please try again.",
        "unknownError": "An error occurred while generating the video. Please try again."
      }
    }
    ```

    Rules: identical to task 1.
  </action>

  <verify>
    Run: `node -e "const f = require('./messages/en.json'); console.log(JSON.stringify(f.errors.kling))" 2>&1`

    Expected output: JSON string with all five kling error keys.

    Run: `node -e "JSON.parse(require('fs').readFileSync('./messages/en.json', 'utf8')); console.log('Valid JSON')" 2>&1`

    Expected: "Valid JSON"
  </verify>

  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('./messages/en.json', 'utf8')); console.log('ok')"` outputs "ok" (valid JSON)
    - `grep '"rateLimit"' messages/en.json` returns a result with English text
    - `grep '"contentPolicy"' messages/en.json` returns a result with English text
    - `grep '"modelUnavailable"' messages/en.json` returns a result with English text
    - `grep '"generationFailed"' messages/en.json` returns a result with English text
    - `grep '"unknownError"' messages/en.json` returns a result with English text
    - `grep '"videoList"' messages/en.json` still returns a result (existing key preserved)
    - `grep '"common"' messages/en.json` still returns a result (existing key preserved)
  </acceptance_criteria>

  <done>
    messages/en.json contains errors.kling with five English error messages. File is valid JSON. All existing keys unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n keys → UI rendering | Error key strings from API response are looked up in message files; no user input involved in key lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Error message content | accept | Messages are user-facing descriptions ("rate limit reached") — not internal error details; no API keys, stack traces, or system info in the message strings |
| T-05-02 | Tampering | Invalid JSON in message files | mitigate | Acceptance criteria include JSON validation via node -e JSON.parse(); a broken JSON file causes next-intl to fail at build time, caught before deployment |
</threat_model>

<verification>
After both tasks:
1. `node -e "JSON.parse(require('fs').readFileSync('./messages/es.json', 'utf8')); console.log('ok')"` — outputs "ok"
2. `node -e "JSON.parse(require('fs').readFileSync('./messages/en.json', 'utf8')); console.log('ok')"` — outputs "ok"
3. `node -e "const f = require('./messages/es.json'); console.log(Object.keys(f.errors.kling))"` — outputs all five keys
4. `node -e "const f = require('./messages/en.json'); console.log(Object.keys(f.errors.kling))"` — outputs all five keys
5. `grep '"videoList"' messages/es.json messages/en.json` — both files still have the key
</verification>

<success_criteria>
- Both es.json and en.json have top-level "errors.kling" with five message keys
- Both files parse as valid JSON
- The five keys match the error key strings returned by app/api/generate/route.ts: rateLimit, contentPolicy, modelUnavailable, generationFailed, unknownError
- No existing i18n keys modified
</success_criteria>

<output>
After completion, create `.planning/phases/01-migrate-sora-to-kling-ai/01-05-SUMMARY.md`
</output>
