---
phase: 03-security-storage-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/video/[videoId]/content/route.ts
  - .env.dev
autonomous: true
requirements:
  - VID-04
  - INFRA-01

must_haves:
  truths:
    - "GET /api/video/{id}/content returns a 302 redirect to the Supabase Storage URL for completed videos owned by the requesting user"
    - "The OpenAI proxy code is fully removed — no fetch to api.openai.com remains"
    - "NEXT_PUBLIC_APP_URL is present in .env.dev"
  artifacts:
    - path: "app/api/video/[videoId]/content/route.ts"
      provides: "Video proxy that redirects to Supabase Storage URL"
      contains: "NextResponse.redirect"
    - path: ".env.dev"
      provides: "Dev environment reference"
      contains: "NEXT_PUBLIC_APP_URL=http://localhost:3000"
  key_links:
    - from: "app/api/video/[videoId]/content/route.ts"
      to: "video_history.video_url"
      via: "supabase select"
      pattern: "select.*video_url"
    - from: "app/api/video/[videoId]/content/route.ts"
      to: "NextResponse.redirect"
      via: "302 redirect"
      pattern: "NextResponse\\.redirect"
---

<objective>
Fix the broken video proxy route and add the missing `NEXT_PUBLIC_APP_URL` env var.

Purpose: The video proxy currently fetches from `https://api.openai.com/v1/videos/{job_id}/content` — a dead endpoint since Phase 1 migrated to Kling AI. Videos are now stored in Supabase Storage and `video_history.video_url` holds the public URL. The fix is a redirect. Additionally, `NEXT_PUBLIC_APP_URL` is missing from `.env.dev`; its absence in production causes the Kling webhook callback URL to be empty, leaving all video generations stuck in "processing".

Output: Fixed `route.ts` that selects `video_url` and redirects. Updated `.env.dev` with `NEXT_PUBLIC_APP_URL`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-security-storage-launch/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix video proxy — select video_url and redirect instead of proxying OpenAI</name>
  <files>app/api/video/[videoId]/content/route.ts</files>
  <read_first>
    - app/api/video/[videoId]/content/route.ts — read the FULL current file before making any changes; extract the auth check, ownership check, and status check — these are preserved verbatim
  </read_first>
  <action>
Replace the file contents. Keep the following unchanged:
  - Import block (NextRequest, NextResponse, createClient)
  - Auth check: `supabase.auth.getUser()` → 401 if missing
  - Ownership check: `.from("video_history").eq("id", videoId).eq("user_id", authUser.id)`
  - Status check: `if (video.status !== "completed")` → 400

Change:
  1. In the `.select()` call: change `"job_id, user_id, status"` to `"video_url, user_id, status"`
  2. Remove the `if (!video.job_id)` null check block
  3. Add null check for `video_url`:
     ```typescript
     if (!video.video_url) {
       return NextResponse.json(
         { error: "Video URL not found" },
         { status: 400 }
       );
     }
     ```
  4. Replace the entire OpenAI fetch block (lines doing `const contentUrl = ...`, `const openaiResponse = await fetch(...)`, blob handling, and the final `new NextResponse(videoBlob, ...)`) with:
     ```typescript
     return NextResponse.redirect(video.video_url, 302);
     ```
  5. Update the catch block log tag from `[v0]` to `[VideoProxy]`

Do NOT add any imports. Do NOT change the function signature or error shape for auth/404/not-ready responses.
  </action>
  <verify>
    <automated>grep -n "video_url\|NextResponse.redirect\|openai\|job_id" /Users/gabrielvega/Projects/agasocial/videoai/video-generator-microsaas/app/api/video/\[videoId\]/content/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "video_url" app/api/video/\[videoId\]/content/route.ts` returns at least 2 matches (select and null check)
    - `grep "NextResponse.redirect" app/api/video/\[videoId\]/content/route.ts` returns exactly 1 match
    - `grep "openai\|api.openai.com\|OPENAI_API_KEY\|job_id" app/api/video/\[videoId\]/content/route.ts` returns 0 matches
    - `grep "VideoProxy" app/api/video/\[videoId\]/content/route.ts` returns at least 1 match (catch block)
  </acceptance_criteria>
  <done>Video proxy selects `video_url` from `video_history` and returns a 302 redirect to the Supabase Storage URL. No OpenAI code remains.</done>
</task>

<task type="auto">
  <name>Task 2: Add NEXT_PUBLIC_APP_URL to .env.dev</name>
  <files>.env.dev</files>
  <read_first>
    - .env.dev — read full file to find the correct insertion point (after Supabase keys, before or after Kling section)
  </read_first>
  <action>
Append `NEXT_PUBLIC_APP_URL=http://localhost:3000` to `.env.dev`.

Insert it immediately after the `SUPABASE_SERVICE_ROLE_KEY` line and before the `# OpenAI` comment, so it sits with the core app configuration. The exact line to add:

```
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

Do NOT modify any existing lines. Do NOT remove `OPENAI_API_KEY` from `.env.dev` (it stays for dev; removal is a production-only runbook step).
  </action>
  <verify>
    <automated>grep "NEXT_PUBLIC_APP_URL" /Users/gabrielvega/Projects/agasocial/videoai/video-generator-microsaas/.env.dev</automated>
  </verify>
  <acceptance_criteria>
    - `grep "NEXT_PUBLIC_APP_URL=http://localhost:3000" .env.dev` returns exactly 1 match
    - `grep "NEXT_PUBLIC_APP_URL" .env.dev | wc -l` outputs `1` (not duplicated)
  </acceptance_criteria>
  <done>`NEXT_PUBLIC_APP_URL=http://localhost:3000` is present in `.env.dev`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/video/[videoId]/content | Authenticated user requests video; videoId is user-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Spoofing | video proxy auth check | mitigate | Supabase `getUser()` verifies JWT on every request; already in code |
| T-03-01-02 | Elevation of Privilege | video ownership check | mitigate | `.eq("user_id", authUser.id)` enforced in DB query; user cannot access other users' videos |
| T-03-01-03 | Information Disclosure | 302 redirect exposes Supabase Storage URL | accept | Bucket is public by design; URL is time-limited by Supabase signed URL if needed in future |
</threat_model>

<verification>
After both tasks:

```bash
# Confirm no OpenAI proxy code remains
grep -c "openai\|api.openai.com\|OPENAI_API_KEY\|job_id" app/api/video/\[videoId\]/content/route.ts
# Expected: 0

# Confirm redirect is in place
grep "NextResponse.redirect" app/api/video/\[videoId\]/content/route.ts
# Expected: return NextResponse.redirect(video.video_url, 302);

# Confirm env var added
grep "NEXT_PUBLIC_APP_URL" .env.dev
# Expected: NEXT_PUBLIC_APP_URL=http://localhost:3000

# Build check
cd /Users/gabrielvega/Projects/agasocial/videoai/video-generator-microsaas && npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
- Video proxy route fetches `video_url` from `video_history` and returns 302 redirect — zero OpenAI dependencies
- `NEXT_PUBLIC_APP_URL=http://localhost:3000` present in `.env.dev`
- TypeScript build passes (`npx tsc --noEmit` exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/03-security-storage-launch/03-01-SUMMARY.md`
</output>
